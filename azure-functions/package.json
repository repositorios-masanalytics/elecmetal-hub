{
  "name": "elecmetal-hub-functions",
  "version": "1.0.0",
  "description": "Azure Functions — Power BI App-Owns-Data embed token service",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build":    "tsc",
    "watch":    "tsc -w",
    "prestart": "npm run build",
    "start":    "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@azure/msal-node": "^2.16.2"
  },
  "devDependencies": {
    "@types/node":  "^20.0.0",
    "typescript":   "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
